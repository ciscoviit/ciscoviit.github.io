import localFont from "next/font/local";

export const exwayer = localFont({
  src: "./exwayer.ttf",
  variable: "--font-exwayer",
});

export const raceSport = localFont({
  src: "./Race Sport.ttf",
  variable: "--font-race-sport",
});
